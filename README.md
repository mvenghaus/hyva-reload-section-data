# Hyvä - Reload Section Data

By default Hyvä loads the customer section data (private content) after every POST Request.

This module disables the reload after AJAX requests to avoid unneccessary server calls. 
